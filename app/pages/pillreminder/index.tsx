import { StyledView, StyledText } from '@components/shared/StyledComponents';
import React from 'react';

const Pillreminder = () => {
  return (
    <StyledView>
      <StyledText>Pill Reminder</StyledText>
    </StyledView>
  );
};

export default Pillreminder;
