import { StyledView, StyledText } from '@components/shared/StyledComponents';
import React from 'react';

const Invitefriends = () => {
  return (
    <StyledView>
      <StyledText>Invite Friends</StyledText>
    </StyledView>
  );
};

export default Invitefriends;
